# Core diffusion & Hugging Face
diffusers
transformers
accelerate
safetensors

# PyTorch (use CPU version if container already has GPU PyTorch)
# For SageMaker GPU container, torch is already installed and matches CUDA
#torch
#torchvision

# Image processing
pillow